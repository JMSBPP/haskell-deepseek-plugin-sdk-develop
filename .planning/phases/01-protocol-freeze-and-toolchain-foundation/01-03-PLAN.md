---
phase: 01-protocol-freeze-and-toolchain-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/README.md
  - docs/adr/0001-harness-e2e-tiering.md
  - .github/workflows/ci.yml
  - .hlint.yaml
  - fourmolu.yaml
  - README.md
autonomous: true
requirements: [TOOL-02, E2E-03]

must_haves:
  truths:
    - "A push or pull request on either remote runs a job that builds, runs the conformance suite, and reports hlint and fourmolu results"
    - "The conformance job is blocking; only the hlint and fourmolu steps are advisory, and the comment says which phase flips them"
    - "A written maintainer decision states which e2e tier deepseek-harness CI accepts, and the eventual bridge PR can cite it by path"
    - "A contributor with only ghcup and stack can read the README and build, test, and format the repository"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "the single workflow definition pushed to both remotes"
      contains: "haskell-actions/setup@v2.12.0"
    - path: "docs/adr/0001-harness-e2e-tiering.md"
      provides: "the E2E-03 decision in MADR 4.0.0 form"
      contains: "### Confirmation"
    - path: "docs/adr/README.md"
      provides: "the ADR index and the declared format, so ADR 0002 is not a format argument"
    - path: ".hlint.yaml"
      provides: "the -XGHC2024 argument hlint needs to parse the sources"
      contains: "-XGHC2024"
    - path: "fourmolu.yaml"
      provides: "the formatter config, generated by fourmolu 0.20.1.0 --print-defaults"
      contains: "indentation: 4"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-red-visible.sh"
      via: "the 'red state is visible' step, blocking"
      pattern: "check-red-visible.sh"
    - from: ".github/workflows/ci.yml"
      to: "haskell-actions/run-fourmolu@v13"
      via: "advisory formatting step with an explicit -XGHC2024"
      pattern: "ghc-opt -XGHC2024"
    - from: "docs/adr/0001-harness-e2e-tiering.md"
      to: "deepseek-harness docs/testing.md"
      via: "the Confirmation section argues the exception against the quoted policy"
      pattern: "docs/testing.md"
---

<objective>
Put the repository under CI and record the one cross-repo decision Phase 10 depends on: `.github/workflows/ci.yml`, the linter configuration both tools need to parse `GHC2024`, and `docs/adr/0001-harness-e2e-tiering.md`.

Purpose: TOOL-02 makes every later phase's green claim checkable by a machine. E2E-03 unblocks Phase 10 — the harness's CI has no GHC anywhere, so the e2e tier must be settled before the bridge PR exists, not during review.
Output: the workflow, `.hlint.yaml`, `fourmolu.yaml`, the ADR index and ADR 0001, and a README a contributor can actually follow.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-protocol-freeze-and-toolchain-foundation/01-CONTEXT.md
@.planning/phases/01-protocol-freeze-and-toolchain-foundation/01-RESEARCH.md
@.planning/research/PITFALLS.md

<verified_facts>
Checked against the GitHub API and by running the binaries on this machine on
2026-08-25:

- `haskell-actions/setup@v2.12.0` declares `runs.using: node24` and its
  `versions.json` lists GHC `9.10.3`. `haskell-actions/run-fourmolu@v13` is
  node24 and accepts the inputs `pattern`, `extra-args`, `version`,
  `working-directory`, `follow-symbolic-links`.
- `haskell-actions/hlint-setup@v2.4.10` and `hlint-run@v2.4.10` still declare
  `runs.using: node20`, and **GitHub removes Node 20 from the runners on
  2026-09-16**. Their `main` branches are node24 but carry no tag. Do not use the
  tagged actions; download the release binary instead.
- `hlint 3.10` release asset `hlint-3.10-x86_64-linux.tar.gz` exists and the
  binary reports `HLint v3.10`. With `.hlint.yaml` containing
  `- arguments: [-XGHC2024]`, `hlint src app test` on this repository's sources
  reports **"No hints"** and exits 0. (The research flagged this flag spelling as
  unverified; it is verified now.)
- `fourmolu 0.20.1.0` ships as **`fourmolu-0.20.1.0-linux-x86_64.zip`** (a zip
  containing a directory with the `fourmolu` binary), not a tarball.
  `fourmolu --version` reports `0.20.1.0 … using ghc-lib-parser 9.14.1.20251220`.
  `fourmolu --mode check --ghc-opt -XGHC2024` on the sources plans 01 and 04
  write exits 0 against the defaults below.
- `stack test --test-arguments="--no-create"` **fails in Phase 1** with
  `Invalid option '--no-create'`. `--no-create` is contributed by the
  `tasty-golden` ingredient, which is only registered once a golden test exists
  in the tree. The first golden lands in Phase 5; do not pass the flag before
  then. (This corrects the command printed in `01-RESEARCH.md` §Code Examples.)
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the ADR index and ADR 0001 recording the e2e tiering decision</name>
  <files>docs/adr/README.md, docs/adr/0001-harness-e2e-tiering.md</files>
  <read_first>
    - `.planning/phases/01-protocol-freeze-and-toolchain-foundation/01-RESEARCH.md` §The ADR (lines 574–604) — the MADR 4.0.0 key names, the three options, and the verbatim `docs/testing.md` clauses
    - `.planning/research/PITFALLS.md` §Pitfall 15 (line 482) — the "keyless snapshot needs GHC, but CI has no GHC" trap
    - `.planning/REQUIREMENTS.md` E2E-01, E2E-02, E2E-03, BRIDGE-09
    - `.planning/ROADMAP.md` Phase 10
  </read_first>
  <action>
Create `docs/adr/README.md`:

State that this directory holds Architecture Decision Records in **MADR 4.0.0**
format, that files are named `NNNN-kebab-title.md` with a zero-padded sequential
number, that an accepted ADR is never edited except to change its `status` (a
reversal is a new ADR that supersedes it), and list the current records as a
table with number, title, and status. Keep it under 30 lines.

Create `docs/adr/0001-harness-e2e-tiering.md` in MADR 4.0.0 form. Front matter
uses the 4.0.0 key names (`status`, `date`, `decision-makers`, `consulted`,
`informed`) — not the MADR 3.x names (`Deciders`, `Validation`):

```markdown
---
status: accepted
date: 2026-08-25
decision-makers: repository owner (both haskell-deepseek-plugin-sdk and deepseek-harness)
consulted: —
informed: —
---

# End-to-end validation tier accepted by deepseek-harness CI
```

Then these sections, with `## Decision Outcome` and `### Confirmation` present
verbatim as headings:

**## Context and Problem Statement.** deepseek-harness CI has no Haskell
anywhere: a grep across all 18 workflow files finds no GHC, Stack, or ghcup step.
Meanwhile `deepseek-harness/docs/testing.md` requires, verbatim:

> Every non-trivial model-, protocol-, or human-visible change adds or updates a
> keyless scenario in the same PR through a runnable example's owning snapshot
> suite. Package tests, e2e assertions, mock/test-only compositions, and PR
> rationale do not replace the assembled transcript

and separately "Prefer the real implementation over a mock: Mock only the
expensive or non-deterministic boundary (LLM adapter, network, clock); keep
everything downstream real." The bridge PR (Phase 10) must therefore ship a
keyless snapshot of a model calling a remote plugin tool — which appears to
require a Haskell binary inside a repository whose CI cannot build one. This ADR
settles which tier of e2e that CI accepts, before the bridge PR exists.

**## Decision Drivers.** The bridge PR must pass the harness's own gates
unchanged. Snapshot replay must be deterministic on macOS and Linux. A
cross-repo binary dependency in CI is a standing maintenance cost. The Haskell
side must still be proven against a real process somewhere.

**## Considered Options.**
- (a) Add a GHC/Stack job to deepseek-harness CI.
- (b) Commit a prebuilt `dsh-plugin-echo` binary to deepseek-harness.
- (c) A ~50-line Node fixture plugin replaying `corpus/plugin.jsonl` as the
  sanctioned harness-CI path, with the real-binary tiers living in this
  repository.

**## Decision Outcome.** Chosen option: **(c)**, the Node fixture plugin. The
harness's keyless snapshot drives the bridge against a fixture that replays the
shared corpus; this repository owns E2E-01 (keyless, the built binary through the
fake host) and E2E-02 (keyed, `dsh --profile headless` against the real binary).

**### Consequences.** Good: the harness gains no toolchain and no cross-repo
build dependency, and its snapshot stays fast and deterministic. Bad: the
harness's snapshot never exercises the real Haskell process, so a Haskell-only
regression is caught in this repository rather than in the harness's CI. Neutral:
the corpus becomes a versioned cross-repo artifact that must be vendored with a
checksum.

**### Confirmation.** This is the section that makes the decision more than an
opinion — it is the argued exception to "prefer the real implementation", and it
rests on the corpus being a *shared artifact* rather than a mock:
- The Node fixture replays the same `corpus/plugin.jsonl` bytes the Haskell
  `conformance` suite replays, so "the fixture and the real plugin agree" is a
  checked fact rather than an assumption.
- The bridge PR vendors the corpus into deepseek-harness with a checksum test, so
  drift fails loud (Phase 8).
- `--dump-manifest` output is diffed against the harness's own
  `assertSupportedJsonSchema`, following the `code-runtime-python`
  `protocol-mirror.e2e.ts` pattern, so the Haskell schema is proven against the
  real validator.
- This repository owns both real-binary tiers: E2E-01 keyless and E2E-02 keyed.

**## Pros and Cons of the Options.** For (a), note the precedent honestly:
`python-runtime` is a distinct required PR job at
`deepseek-harness/.github/workflows/ci.yml:301` (reusing
`build-exe-for-python-sdk.yml`) that exists solely to run the Python SDK
snapshot, and it is listed in the aggregate gate's `needs` at `ci.yml:481`. So
option (a) is not unprecedented — it is rejected on cost, not on principle. For
(b), a platform matrix, binary size in git, and provenance make it unattractive.

**## More Information.** Link `deepseek-harness/docs/testing.md`,
`.agents/notes/implemented/testing/2026-06-19-real-api-e2e-ci.md`, and
`.github/workflows/ci.yml:301`. State the revisit trigger: if deepseek-harness
ever gains a GHC job for another reason, option (a) supersedes this decision and
this ADR is superseded by a new one.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/ai-agents/haskell-deepseek-plugin-sdk && test -f docs/adr/README.md && test -f docs/adr/0001-harness-e2e-tiering.md && grep -q '^status: accepted' docs/adr/0001-harness-e2e-tiering.md && grep -q '^decision-makers:' docs/adr/0001-harness-e2e-tiering.md && grep -q '^## Decision Outcome' docs/adr/0001-harness-e2e-tiering.md && grep -q '^### Confirmation' docs/adr/0001-harness-e2e-tiering.md && grep -q 'docs/testing.md' docs/adr/0001-harness-e2e-tiering.md && grep -q 'python-runtime' docs/adr/0001-harness-e2e-tiering.md && grep -q 'MADR 4.0.0' docs/adr/README.md && echo TASK1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/README.md` exists and contains the literal string `MADR 4.0.0`
    - `docs/adr/0001-harness-e2e-tiering.md` exists
    - `grep -q '^status: accepted'` and `grep -q '^date: 2026-08-25'` and `grep -q '^decision-makers:'` all exit 0 on ADR 0001
    - `grep -q '^Deciders' docs/adr/0001-harness-e2e-tiering.md` exits 1 (MADR 3.x key names are not used)
    - Headings `## Context and Problem Statement`, `## Considered Options`, `## Decision Outcome`, `### Confirmation` are all present
    - `grep -q 'docs/testing.md'` and `grep -q 'python-runtime'` and `grep -q 'ci.yml:301'` all exit 0 on ADR 0001
    - `grep -q 'corpus/plugin.jsonl' docs/adr/0001-harness-e2e-tiering.md` exits 0 — the Confirmation names the shared artifact
    - `grep -qi 'E2E-01' docs/adr/0001-harness-e2e-tiering.md` and `grep -qi 'E2E-02' docs/adr/0001-harness-e2e-tiering.md` exit 0
  </acceptance_criteria>
  <done>A referenceable written decision exists stating that deepseek-harness CI runs the Node fixture snapshot and this repository owns the real-binary e2e tiers, with the argued exception to the harness's "prefer the real implementation" policy spelled out under `### Confirmation`.</done>
</task>

<task type="auto">
  <name>Task 2: Write the CI workflow and the hlint configuration</name>
  <files>.github/workflows/ci.yml, .hlint.yaml</files>
  <read_first>
    - `.planning/phases/01-protocol-freeze-and-toolchain-foundation/01-RESEARCH.md` §GitHub Actions (lines 480–572) and §Code Examples `.github/workflows/ci.yml` (lines 932–1006)
    - The `<verified_facts>` block in this plan — three of its findings correct the research
    - `package.yaml` from plan 01 (the cache key hashes it)
  </read_first>
  <action>
Create `.github/workflows/ci.yml` with exactly this content:

```yaml
name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

# Linux only in Phase 1. This phase ships no transport, so a Windows/macOS matrix
# would cost runner time for no signal. The matrix lands in Phase 2 together with
# the NDJSON transport, where newline mode, process termination, and GHC console
# handling first differ by platform.
jobs:
  build-and-test:
    name: stack build + conformance (ubuntu, ghc 9.10.3)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - id: setup
        uses: haskell-actions/setup@v2.12.0
        with:
          ghc-version: '9.10.3'
          enable-stack: true
          stack-version: 'latest'
          cabal-update: false

      - uses: actions/cache@v4
        with:
          path: |
            ${{ steps.setup.outputs.stack-root }}
            .stack-work
          key: ${{ runner.os }}-stack-${{ hashFiles('stack.yaml.lock', 'package.yaml') }}
          restore-keys: ${{ runner.os }}-stack-

      - name: build dependencies
        run: stack build --system-ghc --no-install-ghc --test --only-dependencies

      - name: build
        run: stack build --system-ghc --no-install-ghc --pedantic --test --no-run-tests

      - name: generated .cabal is current
        run: git diff --exit-code -- '*.cabal'

      - name: conformance
        shell: bash
        run: |
          set -o pipefail
          stack test --system-ghc --no-install-ghc --pedantic 2>&1 | tee conformance.log

      - name: red state is visible
        run: scripts/check-red-visible.sh conformance.log

      # Advisory until Phase 7 removes continue-on-error from both steps, in the
      # same change that deletes the last corpus EXPECTED.md. Neither tool reads
      # default-language from the .cabal, so both are told about GHC2024.
      - name: hlint
        continue-on-error: true
        run: |
          curl -fsSL https://github.com/ndmitchell/hlint/releases/download/v3.10/hlint-3.10-x86_64-linux.tar.gz \
            | tar xz --strip-components=1 -C "$RUNNER_TEMP" hlint-3.10/hlint
          "$RUNNER_TEMP/hlint" src app test

      - name: fourmolu
        continue-on-error: true
        uses: haskell-actions/run-fourmolu@v13
        with:
          version: '0.20.1.0'
          extra-args: '--ghc-opt -XGHC2024'
          pattern: |
            src/**/*.hs
            app/**/*.hs
            test/**/*.hs
```

Four things in there are deliberate and must not be "simplified":

1. `--system-ghc --no-install-ghc` are **CLI flags, not `stack.yaml` keys**, so a
   clean local clone still gets a Stack-managed GHC with zero setup (TOOL-03).
2. `--pedantic` appears on both the `build` and `conformance` steps so the two
   invocations share one set of GHC flags and the second reuses the first's
   build artifacts.
3. `stack test` is **not** given `--test-arguments="--no-create"`. That option is
   contributed by the `tasty-golden` ingredient and is rejected as an invalid
   option until a golden test exists in the tree (Phase 5). Phase 5 adds it back.
4. `set -o pipefail` with `shell: bash` is required: the runner's default `bash -e`
   does not set pipefail, so `stack test | tee` would otherwise mask a failure.

`scripts/check-red-visible.sh` is created by plan 04 in wave 2. CI first runs
when plan 05 pushes, so the reference resolves by then.

Create `.hlint.yaml` with exactly:

```yaml
# hlint does not read default-language from the .cabal file, so GHC2024 has to be
# passed explicitly or the parser rejects syntax that compiles cleanly.
- arguments: [-XGHC2024]
```
  </action>
  <verify>
    <automated>cd /home/jmsbpp/ai-agents/haskell-deepseek-plugin-sdk && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); s=d['jobs']['build-and-test']['steps']; names=[x.get('name') for x in s]; assert 'conformance' in names and 'red state is visible' in names, names; c={x.get('name'):x.get('continue-on-error',False) for x in s}; assert c['conformance'] is False and c['red state is visible'] is False, c; assert c['hlint'] is True and c['fourmolu'] is True, c; print('CI_OK')" && grep -q 'haskell-actions/setup@v2.12.0' .github/workflows/ci.yml && grep -q 'run-fourmolu@v13' .github/workflows/ci.yml && ! grep -q 'hlint-setup' .github/workflows/ci.yml && ! grep -q 'no-create' .github/workflows/ci.yml && grep -q -- '-XGHC2024' .hlint.yaml && echo TASK2_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as YAML and `jobs.build-and-test.steps` contains steps named `conformance` and `red state is visible`
    - Neither the `conformance` nor the `red state is visible` step has `continue-on-error`
    - Both the `hlint` and `fourmolu` steps have `continue-on-error: true`
    - `grep -q 'haskell-actions/setup@v2.12.0'` exits 0
    - `grep -q 'haskell-actions/run-fourmolu@v13'` exits 0
    - `grep -q 'hlint-setup\|hlint-run' .github/workflows/ci.yml` exits 1 — the node20 actions are not used
    - `grep -q 'no-create' .github/workflows/ci.yml` exits 1
    - `grep -q 'pull_request' .github/workflows/ci.yml` and `grep -q 'branches: \[main\]' .github/workflows/ci.yml` exit 0
    - `grep -q "git diff --exit-code -- '\*.cabal'" .github/workflows/ci.yml` exits 0
    - `grep -q 'set -o pipefail' .github/workflows/ci.yml` exits 0
    - `grep -qi 'Windows/macOS matrix\|Windows and macOS\|Windows/macOS' .github/workflows/ci.yml` exits 0 — the deferral is recorded in the file
    - `grep -q -- '- arguments: \[-XGHC2024\]' .hlint.yaml` exits 0
  </acceptance_criteria>
  <done>One workflow file builds with `-Werror`, gates on the generated `.cabal` being current, runs the conformance suite as a blocking step, asserts the red state is visible, and reports `hlint` and `fourmolu` advisorily with an explicit `GHC2024`.</done>
</task>

<task type="auto">
  <name>Task 3: Generate fourmolu.yaml from the pinned binary and rewrite the README</name>
  <files>fourmolu.yaml, README.md</files>
  <read_first>
    - `README.md` (currently one line)
    - `.planning/phases/01-protocol-freeze-and-toolchain-foundation/01-RESEARCH.md` §Open Questions Q4 (lines 1063–1065) and Pitfall 6 (lines 647–650)
    - `.github/workflows/ci.yml` from task 2 — the README's commands must be the local equivalents of the CI steps
  </read_first>
  <action>
`fourmolu.yaml` is **generated, never hand-written** — its field set has shifted
across minor releases and a transcribed list goes stale silently. Generate it
from the same version CI pins:

```sh
cd "$(mktemp -d)"
curl -fsSL -o fourmolu.zip https://github.com/fourmolu/fourmolu/releases/download/v0.20.1.0/fourmolu-0.20.1.0-linux-x86_64.zip
unzip -q fourmolu.zip
chmod +x fourmolu-0.20.1.0-linux-x86_64/fourmolu
./fourmolu-0.20.1.0-linux-x86_64/fourmolu --version   # expect 0.20.1.0
./fourmolu-0.20.1.0-linux-x86_64/fourmolu --print-defaults > /path/to/repo/fourmolu.yaml
```

The asset is a **zip containing a directory**, not a tarball — the research's
tarball assumption is wrong. If the download fails, stop and report the blocker
rather than hand-writing the file; a hand-written config that disagrees with the
pinned binary makes the Phase 7 flip-to-blocking a silent reformat of the whole
tree.

Cross-check: the generated file must begin with

```yaml
# Number of spaces per indentation step
indentation: 4
```

and must contain `column-limit: none`, `function-arrows: trailing`,
`import-export-style: diff-friendly`, `haddock-style: multi-line`,
`single-constraint-parens: always`, and `respectful: true`. If your output has
`indentation: 2` you downloaded a different version; the research's transcribed
block is from an older release and is not the target.

Then rewrite `README.md`. Keep it short and current-state — what is true today,
not a plan. Cover:

**Title and one-paragraph purpose.** A Haskell SDK for writing DeepSeek Harness
out-of-process plugins, plus the TypeScript bridge that mounts them. State the
status plainly: the wire is frozen in `PROTOCOL.md` and pinned by `corpus/`;
no runtime is implemented yet, and the conformance suite is deliberately red.

**Requirements.** ghcup and stack, nothing else. GHC 9.10.3 comes from the
`lts-24.56` snapshot. Note the cabal-only caveat: the generated `.cabal` declares
`default-language: GHC2024`, which needs **Cabal ≥ 3.12** to parse, so a
`cabal-install` older than that will fail even though Stack is fine.

**Commands.**

```sh
stack build                 # library + dsh-plugin-echo + conformance suite
stack test                  # the conformance suite
stack test 2>&1 | tee conformance.log && scripts/check-red-visible.sh conformance.log
stack build --pedantic --test --no-run-tests   # what CI builds: -Wall -Werror
```

Note that `stack test --test-arguments="--no-create"` does not work until a
golden test exists (Phase 5); until then the flag is rejected as invalid.

**Reading the red state.** Explain the contract in three sentences: a scenario is
expect-fail wrapped if and only if `corpus/<scenario>/EXPECTED.md` exists; its
first line names the phase that must turn it green; and when a wrapped scenario
starts passing, tasty reports `OK (unexpected: …)` and **fails the suite**, which
is what forces the implementing phase to delete the file.

**Repository layout.** A short list: `PROTOCOL.md` (the frozen wire), `corpus/`
(the executable spec both implementations replay), `src/` (the library),
`app/echo/` (the example binary), `test/` (the conformance suite), `docs/adr/`
(decision records), `scripts/`.

**Formatting and linting.** State that `fourmolu.yaml` is the generated defaults
of fourmolu 0.20.1.0 and `.hlint.yaml` only adds `-XGHC2024`; both run advisorily
in CI until Phase 7 makes them blocking.

Do not add CI status badges yet — plan 05 enables Actions on both remotes, and a
badge for a disabled workflow renders as an error.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/ai-agents/haskell-deepseek-plugin-sdk && grep -q '^indentation: 4$' fourmolu.yaml && grep -q '^column-limit: none$' fourmolu.yaml && grep -q '^import-export-style: diff-friendly$' fourmolu.yaml && grep -q '^haddock-style: multi-line$' fourmolu.yaml && grep -q 'lts-24.56\|9.10.3' README.md && grep -q 'stack test' README.md && grep -q 'EXPECTED.md' README.md && grep -q 'PROTOCOL.md' README.md && test "$(wc -l < README.md)" -gt 20 && echo TASK3_OK</automated>
  </verify>
  <acceptance_criteria>
    - `fourmolu.yaml` exists and its first non-comment line is `indentation: 4`
    - `fourmolu.yaml` contains all of: `column-limit: none`, `function-arrows: trailing`, `comma-style: leading`, `import-export-style: diff-friendly`, `haddock-style: multi-line`, `single-constraint-parens: always`, `respectful: true`
    - `grep -q '^indentation: 2' fourmolu.yaml` exits 1 — the stale transcribed defaults were not used
    - `README.md` is more than 20 lines and mentions `PROTOCOL.md`, `corpus/`, `EXPECTED.md`, `stack build`, `stack test`, `scripts/check-red-visible.sh`, and `ghcup`
    - `grep -q 'Cabal' README.md` exits 0 and the cabal-install ≥ 3.12 caveat is stated
    - `grep -qi 'OK (unexpected' README.md` exits 0 — the forcing function is explained
    - `grep -q 'badge\|shields.io' README.md` exits 1
  </acceptance_criteria>
  <done>`fourmolu.yaml` is the verbatim output of `fourmolu 0.20.1.0 --print-defaults`, and a contributor with only ghcup and stack can follow the README to build, test, read the deliberate red state, and format.</done>
</task>

</tasks>

<verification>
```sh
cd /home/jmsbpp/ai-agents/haskell-deepseek-plugin-sdk
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"   # parses
grep -q 'run-fourmolu@v13' .github/workflows/ci.yml
! grep -q 'hlint-setup' .github/workflows/ci.yml
grep -q '^indentation: 4' fourmolu.yaml
grep -q '^### Confirmation' docs/adr/0001-harness-e2e-tiering.md
```
</verification>

<success_criteria>
- `.github/workflows/ci.yml` parses, triggers on push to `main` and on pull requests, builds with `--pedantic`, gates on `.cabal` freshness, runs the conformance suite blocking, and reports `hlint`/`fourmolu` advisorily
- No node20 action is referenced anywhere in the workflow
- `.hlint.yaml` and `fourmolu.yaml` both exist, `fourmolu.yaml` generated by the pinned 0.20.1.0 binary
- `docs/adr/0001-harness-e2e-tiering.md` records the E2E-03 decision in MADR 4.0.0 form with a `### Confirmation` section that argues the exception to the harness's testing policy
- `README.md` gets a contributor from a clean clone to a green `stack test` with a visible red state
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-freeze-and-toolchain-foundation/01-03-SUMMARY.md`
</output>
